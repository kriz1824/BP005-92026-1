const int LED = 2;
void setup() {
    pinMode(LED, OUTPUT);
}

void loop() {
  int contador = 1;

      while (contador <= 5) {
      digitalWrite(LED, HIGH); 
       delay (250);
      
       digitalWrite(LED, LOW);
      delay(250);

      contador++;
}

delay(1500); 
}
